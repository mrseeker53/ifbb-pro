import React from 'react';
import './Workout.css'

const Workout = (props) => {
    const { workout } = props;
    const { picture, name, description } = workout;

    return (
        <div>
            
        </div>

    );
};

export default Workout;